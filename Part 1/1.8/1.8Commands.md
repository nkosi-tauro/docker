Commands : 
```
$ docker run -d -it missing1.8 ubuntu 
$ docker exec -it missing1.8 bash
$ apt update
$ apt install curl
$ sh -c 'echo "Input website:"; read website; echo "Searching.."; sleep 1; curl http://www.$website;
$ Input website : helsinki.fi
```